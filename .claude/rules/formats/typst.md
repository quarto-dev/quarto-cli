---
paths:
  - "src/format/typst/**"
  - "src/resources/formats/typst/**"
---

# Typst Format

Guidance for working with Quarto's Typst format support.

For Pandoc template integration (how Pandoc's Typst templates are copied into Quarto and restructured into a modular form), see `llm-docs/pandoc-quarto-typst-templates.md`.

project:
  type: website

website:
  title: "<%= title %>"
  navbar:
    type: dark
    background: primary
    left:
      - href: index<%= ext %>
        text: Home
      - about<%= ext %>

format:
  html:
    theme: cosmo
    css: styles.css

<% if(editor){ %>editor: <%= editor %><% } %>

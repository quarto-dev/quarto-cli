project:
  type: website

site:
  title: "<%= title %>"
  navbar:
    type: dark
    background: primary
    left:
      - href: index<%= ext %>
        text: Home
      - about<%= ext %>

format:
  html:
    theme: cosmo
    css: styles.css
    toc: true

<% if(editor){ %>editor: <%= editor %><% } %>

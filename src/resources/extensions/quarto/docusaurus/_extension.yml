title: Docusaurus
author: RStudio, PBC
organization: quarto
contributes:
  project:
    project:
      type: default
      detect: "docusaurus.config.js"
      render:
        - "**/*.qmd"
        - "**/*.ipynb"
      preview:
        serve:
          cmd: "npm run docusaurus start -- --no-open --port {port} --host {host}"
          ready: "compiled successfully"
    format: docusaurus-commonmark
  formats:
    commonmark:
      variant: +tex_math_dollars
      keep-yaml: true
      wrap: none
      filters:
        - docusaurus.lua

<style type="text/css">
  .navbar-brand>img {
    max-height: 24px;
    width: auto;
    padding-right: 6px;
  }

  :target::before {
    content: "";
    display: block;
    height: <%=height %>px;
    margin: -<%=height %>px 0 0;
  }

  body {
    padding-top: <%=height %>px;
  }

  .sidebar {
    position: sticky;
    max-height: calc(100vh - <%=height %>px);
    top: <%=height %>px;
    overflow-y: auto;
    max-width: 15rem;
  }

  .quarto-btn-toggle[aria-expanded=false] .bi-arrows-collapse {
    display: none;
  }

  .quarto-btn-toggle[aria-expanded=true] .bi-arrows-expand {
    display: none;
  }

  .quarto-secondary-nav {
    margin-top: -4px;
  }

  .quarto-secondary-nav-title {
    padding-top: 4px;
    margin-bottom: -4px;
  }

  @include media-breakpoint-down(lg) :not(.show) {
    #quarto-sidebar {
      height: 0;
    }
  }

  @include media-breakpoint-down(lg) {
    #quarto-sidebar {
      border-bottom: 1px solid var(--bs-light);
    }
  }

  @include media-breakpoint-up(lg) {
    #quarto-sidebar {
      height: calc(100vh - 7rem);
      display: block;
    }
  }

  .quarto-sidebar-logo {
    max-width: 200px;
  }
</style>
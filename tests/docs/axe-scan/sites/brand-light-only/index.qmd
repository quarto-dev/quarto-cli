---
title: Home
---

This site's `_brand.yml` sets light values only, so Quarto emits
`data-mode="light"` stylesheet links, no `data-mode="dark"` build and no
colour-scheme toggle.

```{=html}
<!-- PLANTED: button-name (critical). A bare button, so the page has one finding
     to compare between the light cell and the dark cell that reuses it. Reuse
     that returned an empty result would look identical to a clean page, so the
     comparison needs something to find. -->
<p><button></button></p>
```

Keep this page's markup minimal. Anything that adds a second, conditional
finding would blur the one comparison the site exists to make.

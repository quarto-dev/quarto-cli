---
format: html
shortcodes:
  - typst-path-api.lua
_quarto:
  tests:
    html:
      ensureFileRegexMatches:
        - ["<p>The Typst path is <code>.+typst</code>.</p>"]
---

The Typst path is `{{< typst >}}`.

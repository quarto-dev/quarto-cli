---
title: brand font - title is `#context text.font`{=typst} `#context text.size`{=typst}
format: typst
include-in-header:
  text: |
    #set text(fallback: false)
_quarto:
  tests:
    typst:
      ensurePdfRegexMatches:
      -
        - 'brand font - title is roboto slab 16.5pt'
        - 'heading-2 is roboto slab 15.4pt'
        - 'paragraph is open sans 11pt'
        - 'fib = num => num < 2'
      - []
---

## heading-2 is `#context text.font`{=typst} `#context text.size`{=typst}

paragraph is `#context text.font`{=typst} `#context text.size`{=typst}.

```{=typst}
#block(text(font: "Fira Code")[Some text with font set explicitly to #context text.font #context text.size])

```

```
const fib = num => num < 2 ? num : fib(num-1) + fib(num - 2);
console.log(fib(12))
```

{{< lipsum 3 >}}


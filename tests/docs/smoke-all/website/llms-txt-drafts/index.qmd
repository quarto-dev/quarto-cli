---
title: "Draft Test Site"
_quarto:
  render-project: true
  tests:
    html:
      ensureLlmsMdExists: true
      ensureLlmsTxtExists: true
      ensureLlmsTxtRegexMatches:
        # First array: patterns that MUST match (index page should be in llms.txt)
        - ["\\[Draft Test Site\\]"]
        # Second array: patterns that must NOT match (draft page should NOT be in llms.txt)
        - ["\\[Draft Page\\]", "draft-page\\.llms\\.md"]
---

This is the index page.

The [draft page](draft-page.qmd) should not appear in llms.txt.

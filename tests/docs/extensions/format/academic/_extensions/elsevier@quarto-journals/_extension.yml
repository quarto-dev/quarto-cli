name: Elsevier Journal Format
contributes:
  format:
    common:
      date-format: full
    pdf:
      citation-package: natbib
      template-partials:
        [frontmatter.tex, doc-class.tex, title.tex, elsarticle.cls]
      classoption: preprint, 3p, authoryear
      number-sections: true
